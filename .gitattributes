# 所有文件使用 LF 换行符
* text=auto eol=lf

# Shell 脚本必须使用 LF
*.sh text eol=lf

# YAML 文件（GitHub Actions）必须使用 LF
*.yml text eol=lf
*.yaml text eol=lf

# Windows 批处理使用 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
